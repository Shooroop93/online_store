spring:
  application:
    name: online_store
  liquibase:
    enabled: true
  datasource:
    url:
    username:
    password:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none
server:
  port: 8081